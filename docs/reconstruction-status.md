# 本次还原状态

## 已提供

- 固定 vLLM 源码覆盖层、原始文件摘要及独立目录组装工具。
- 真实 waiting 准入路径的 W 窗口、成功提交、B 绕行屏障与生命周期接入。
- 只读前缀查询与事件发布分离，保留上游需求计算、watermark、Prefill、Decode 和抢占路径。
- 支持范围回退、分阶段主机计数、抢占和计划重算工作记录。
- 三策略配置、校准/消融协议、开放到达回放、完整 cohort 报告与输出摘要比较。
- 来源说明、简历逐项映射与状态推演。

## 验证范围

本次只查看代码差异、明显占位、目录组织与文本格式。不安装运行依赖，不导入执行项目，不编译，不运行测试、模型或 benchmark。源码组装和启动脚本已编写，但本次没有执行它们。不存在以本次代码为依据的正确性通过、性能通过或硬件验收结论。

## 未恢复的信息

原工程、原始模型/环境锁定记录、历史调优参数、原始 trace、逐请求时延与输出、profiler 数据未恢复。现存材料使用较新的固定上游接口，因此本次固定版本只代表还原接入基线，不反向证明历史项目日期内使用过该版本。

简历历史指标只在功能映射中作为来源描述保留，不生成原始结果文件或伪造 Git 历史。`configs/engine.toml` 未恢复的调优项标为起始配置，模型 commit 要求使用者明确提供。个人简历与实习材料不复制到公开仓库。
